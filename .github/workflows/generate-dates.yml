name: Generate dates index

on:
  push:
    paths:
      - "data/*.json"
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Generate dates.json
        run: |
          ls data/*.json \
            | sed 's|data/||' \
            | sed 's|\.json||' \
            | grep -E '^[0-9]{4}-[0-9]{2}-[0-9]{2}$' \
            | sort -r \
            | jq -R -s -c 'split("\n")[:-1]' \
            > data/dates.json

      - name: Commit dates.json
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add data/dates.json
          git commit -m "Update dates.json" || exit 0
          git push https://x-access-token:${{ secrets.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 }}@github.com/${{ github.repository }}.git
